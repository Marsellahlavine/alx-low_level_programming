#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: Always 0
 */
int main(void)
{
char letter;
for (letter = 'a'; letter <= 'z'; letter++)
if (a == 'q' || a =='e')
continue;
putchar(letter);
putchar(a);
return (0);
}
